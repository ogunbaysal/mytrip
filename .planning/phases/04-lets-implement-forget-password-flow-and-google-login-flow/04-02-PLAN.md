---
phase: 04-lets-implement-forget-password-flow-and-google-login-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/web/src/app/login/page.tsx
  - apps/web/src/app/forgot-password/page.tsx
  - apps/web/src/app/forgot-password/layout.tsx
  - apps/web/src/app/reset-password/page.tsx
  - apps/web/src/app/reset-password/layout.tsx
  - apps/web/src/lib/auth-client.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "Web users can request a password reset from the login flow."
    - "Users can set a new password using a tokenized reset link."
    - "Users can start Google login from the login page."
  artifacts:
    - path: "apps/web/src/app/forgot-password/page.tsx"
      provides: "Forgot password form and reset email request"
    - path: "apps/web/src/app/reset-password/page.tsx"
      provides: "Reset password form that submits token + new password"
    - path: "apps/web/src/app/login/page.tsx"
      provides: "Login page links to forgot password and Google login"
  key_links:
    - from: "apps/web/src/app/forgot-password/page.tsx"
      to: "authClient.requestPasswordReset"
      via: "mutation submit"
      pattern: "requestPasswordReset"
    - from: "apps/web/src/app/reset-password/page.tsx"
      to: "authClient.resetPassword"
      via: "mutation submit"
      pattern: "resetPassword"
    - from: "apps/web/src/app/login/page.tsx"
      to: "authClient.signIn.social"
      via: "Google button"
      pattern: "signIn\.social"
---

<objective>
Add web-only forgot/reset password pages and Google login entry points.

Purpose: Complete user-facing flows for password recovery and Google sign-in.
Output: New auth pages + login updates using existing Better Auth web client.
</objective>

<execution_context>
@/Users/ogun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ogun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/app/login/page.tsx
@apps/web/src/lib/auth-client.ts
@apps/web/src/lib/redirect.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add forgot/reset password pages</name>
  <files>apps/web/src/app/forgot-password/page.tsx, apps/web/src/app/forgot-password/layout.tsx, apps/web/src/app/reset-password/page.tsx, apps/web/src/app/reset-password/layout.tsx</files>
  <action>
Create `/forgot-password` and `/reset-password` pages using the same Card + Input + Button patterns as login/register. Use TanStack Query `useMutation` with `authClient.requestPasswordReset({ email, redirectTo })` and `authClient.resetPassword({ token, newPassword })`. The reset page should read `token` and `error` from query params and surface a helpful message if missing/invalid. Keep Turkish copy consistent with existing auth pages. Do not add admin routes or components.
  </action>
  <verify>
    <automated>bunx turbo run type-check --filter=web</automated>
  </verify>
  <done>Forgot/reset pages render, submit mutations, and handle missing/invalid token state.</done>
</task>

<task type="auto">
  <name>Task 2: Add Google login and forgot-password entry points</name>
  <files>apps/web/src/app/login/page.tsx, apps/web/src/lib/auth-client.ts</files>
  <action>
Expose a Google sign-in button on the login page that calls `authClient.signIn.social({ provider: "google", callbackURL: redirectTo, errorCallbackURL: "/login" })`. Add a "Şifremi Unuttum" link below the password field that navigates to `/forgot-password` (include `redirect` query when present, using `resolveSafeRedirect`). If needed, export helper methods from `auth-client.ts` to keep the page clean. Keep styling consistent with existing shadcn Card/Button usage.
  </action>
  <verify>
    <automated>bunx turbo run type-check --filter=web</automated>
  </verify>
  <done>Login page includes Google button and forgot-password link with correct redirect handling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify web auth flows</name>
  <files>apps/web/src/app/login/page.tsx, apps/web/src/app/forgot-password/page.tsx, apps/web/src/app/reset-password/page.tsx</files>
  <action>Manually validate the new forgot/reset password and Google login entry points in the web app.</action>
  <verify>
    <automated>MISSING — Wave 0 must create a UI smoke test for auth pages</automated>
  </verify>
  <done>Login, forgot-password, and reset-password routes render and accept user input without errors.</done>
  <what-built>Web forgot/reset password and Google login entry points</what-built>
  <how-to-verify>
    1. Open `http://localhost:3000/login` and verify Google button + forgot-password link render.
    2. Open `/forgot-password`, submit an email, confirm success toast appears (email delivery depends on SMTP config).
    3. Open `/reset-password?token=INVALID_TOKEN` and confirm error state renders.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `bunx turbo run type-check --filter=web`
</verification>

<success_criteria>

- Forgot/reset password pages work with Better Auth client methods.
- Login page provides Google sign-in and password reset entry.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-lets-implement-forget-password-flow-and-google-login-flow/04-02-SUMMARY.md`
</output>
