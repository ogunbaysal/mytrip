---
phase: 04-lets-implement-forget-password-flow-and-google-login-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/lib/web-auth.ts
  - apps/api/src/lib/email.ts
  - apps/api/.env.example
  - .env.example
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
user_setup:
  - service: google-oauth
    why: "Enable Google social login for web users"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> OAuth 2.0 Client IDs"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console -> OAuth 2.0 Client IDs"
    dashboard_config:
      - task: "Register OAuth redirect URI: {API_BASE_URL}/api/web/auth/callback/google"
        location: "Google Cloud Console -> APIs & Services -> Credentials"
  - service: smtp
    why: "Send password reset emails"
    env_vars:
      - name: SMTP_HOST
        source: "SMTP provider dashboard"
      - name: SMTP_PORT
        source: "SMTP provider dashboard"
      - name: SMTP_USER
        source: "SMTP provider dashboard"
      - name: SMTP_PASS
        source: "SMTP provider dashboard"
      - name: SMTP_FROM
        source: "Verified sender identity"
must_haves:
  truths:
    - "Password reset requests for web users trigger an email send attempt."
    - "Google OAuth is enabled for the web auth namespace."
  artifacts:
    - path: "apps/api/src/lib/web-auth.ts"
      provides: "Better Auth web config with password reset + Google provider"
      contains: "sendResetPassword"
    - path: "apps/api/src/lib/email.ts"
      provides: "SMTP email sender utility for reset emails"
    - path: "apps/api/.env.example"
      provides: "Documented SMTP + Google env vars"
  key_links:
    - from: "apps/api/src/lib/web-auth.ts"
      to: "apps/api/src/lib/email.ts"
      via: "sendResetPassword callback"
      pattern: "sendResetPassword"
    - from: "apps/api/src/lib/web-auth.ts"
      to: "GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET"
      via: "socialProviders.google"
      pattern: "socialProviders\s*:\s*\{\s*google"
---

<objective>
Enable web-only Better Auth password reset emails and Google OAuth configuration on the API.

Purpose: Unlock web forgot-password and Google login flows without touching admin auth.
Output: Web auth config updated, email sender utility, env examples for Google + SMTP.
</objective>

<execution_context>
@/Users/ogun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ogun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api/src/lib/web-auth.ts
@apps/api/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SMTP email sender utility and env docs</name>
  <files>apps/api/src/lib/email.ts, apps/api/package.json, apps/api/.env.example, .env.example</files>
  <action>
Create `apps/api/src/lib/email.ts` with a small Nodemailer-based `sendPasswordResetEmail({ to, resetUrl })` helper. Use SMTP env vars (`SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`), and return a result or throw on failure. Update `apps/api/package.json` to include `nodemailer` dependency. Document the SMTP and Google env vars in `apps/api/.env.example` and root `.env.example` (keep existing vars). Do not add admin scope config.
  </action>
  <verify>
    <automated>bunx turbo run type-check --filter=api</automated>
  </verify>
  <done>SMTP helper exists, Nodemailer dependency added, and env examples include SMTP + Google vars.</done>
</task>

<task type="auto">
  <name>Task 2: Configure web Better Auth for reset + Google</name>
  <files>apps/api/src/lib/web-auth.ts</files>
  <action>
Extend the `webAuth` config to add `socialProviders.google` using `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET`, and wire `emailAndPassword.sendResetPassword` to call the new SMTP helper with the provided `url`. Keep the baseURL `/api/web/auth` and do not touch admin auth (`apps/api/src/lib/auth.ts`). Optionally log in `onPasswordReset` for visibility.
  </action>
  <verify>
    <automated>bunx turbo run type-check --filter=api</automated>
  </verify>
  <done>Web auth config supports Google OAuth and triggers SMTP email on reset request.</done>
</task>

</tasks>

<verification>
- `bunx turbo run type-check --filter=api`
</verification>

<success_criteria>

- Web auth config includes Google provider + reset password email callback.
- SMTP helper and env documentation exist for web-only auth flows.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-lets-implement-forget-password-flow-and-google-login-flow/04-01-SUMMARY.md`
</output>
